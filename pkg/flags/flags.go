package flags

// Flags
const (
	FlagInput   = "input"
	FlagOutput  = "output"
	FlagHelp    = "help"
	FlagVersion = "version"
)

// Description
const (
	DescInput   = "Path to input json file (default = stdin)"
	DescOutput  = "Path to output YAML file (default = stdout)"
	DescHelp    = "Show help message"
	DescVersion = "Show current version"
)

const version = "0.1.0"
